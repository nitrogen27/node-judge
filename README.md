# Online Judge - Проверка решений на Node.js

Веб-приложение для проверки решений задач на Node.js с использованием WebSocket для обмена данными.

https://cdn.jsdelivr.net/npm/monaco-editor@0.54.0/min/vs/loader.js 

## Возможности

- 🔥 Редактор кода с подсветкой синтаксиса (Monaco Editor)
- 📡 WebSocket соединение для реального времени
- ✅ Автоматическое тестирование решений
- 📊 Детальные результаты тестирования
- ⚡ Отображение времени выполнения тестов

## Установка

1. Установите зависимости сервера:
```bash
npm install
```

2. Установите зависимости клиента:
```bash
npm run install-client
```

## Запуск

### Режим разработки (сервер + клиент одновременно)

```bash
npm run dev
```

Сервер будет доступен на `http://localhost:3001`
Клиент будет доступен на `http://localhost:3000`

### Запуск только сервера

```bash
npm run server
```

### Запуск только клиента

```bash
npm run client
```

## Структура проекта

```
node-judge/
├── server.js          # WebSocket сервер
├── judge.js           # Оригинальный judge (CLI версия)
├── solution.js        # Пример решения
├── client/            # React приложение
│   ├── src/
│   │   ├── App.js     # Главный компонент
│   │   ├── App.css    # Стили
│   │   └── index.js   # Точка входа
│   └── public/
└── package.json
```

## Использование

1. Откройте приложение в браузере
2. Введите или отредактируйте код решения в редакторе
3. Нажмите кнопку "Отправить"
4. Дождитесь результатов тестирования

## Тесты

Тесты определены в `server.js`. Вы можете изменить их по своему усмотрению.

## Технологии

- **Backend**: Node.js, Express, WebSocket (ws)
- **Frontend**: React, Monaco Editor
- **Transport**: WebSocket для реального времени

